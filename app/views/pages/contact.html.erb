<div class='container'>
  <h1><%= phrase('contacts.title') %></h1>
  <p class='lead'><%= phrase('contacts.lead') %></p>
  <br class='hidden-sm' />
  <div class='container'>
    <%= render 'shared/flash_message' %>
    <%= render 'pages/contact/contact_form' %>
  </div>
</div>

