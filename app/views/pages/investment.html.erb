<div class="container">
	<h1><%=	phrase('investments.title')%></h1>
	<br>
	<div class='row'>
    <% @investments.each do |investment|  %>
      <div class='col-sm-4'>
        <%= image_tag investment.attachment.url, class: 'circlized-carousel-image'%>
      </div>
		  <div class='col-sm-7 col-sm-offset-1'>
        <%= investment.description(lang).html_safe %>
		  </div>
    <% end %>
	</div>
</div>
