<% if flash.any? %>
  <div class='flash-message to-hide'>
    <% flash.each do |key, value| %>
      <div class="alert flash-msg alert-<%= key %>">
        <%= value.to_s.html_safe %>
      </div>
    <% end %>
  </div>
<% end %>
